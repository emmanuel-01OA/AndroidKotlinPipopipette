<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidkotlinpipopipette">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_pipopipette"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidKotlinPipopipette">
        <activity
            android:name=".SpashAnimationPipopipette"
            android:exported="true"
           >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".configNomJActivity"
            android:exported="false" />
        <activity
            android:name=".MenuPipopipette"
            android:exported="false" />
        <activity
            android:name=".InterfaceJeuPipopipetteActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:label="@string/title_activity_interface_jeu_pipopipette"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AndroidKotlinPipopipette.Fullscreen"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".Menuconfig"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/logo_config" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>